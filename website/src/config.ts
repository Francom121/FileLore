/**
 * Site-wide constants.
 */

/** Direct download for the latest FileLore release (served from /public). */
export const DOWNLOAD_URL = "/downloads/FileLore-macOS.zip"

/** Human-readable size of the download zip, shown next to download buttons. */
export const DOWNLOAD_SIZE = "4.3 MB"

/** Direct download for the latest Windows (x64) installer (served from /public). */
export const DOWNLOAD_URL_WINDOWS = "/downloads/FileLore-Windows-Setup-x64.exe"

/** Human-readable size of the Windows x64 installer. */
export const DOWNLOAD_SIZE_WINDOWS = "78.1 MB"

/** Direct download for the latest Windows on ARM (arm64) installer (served from /public). */
export const DOWNLOAD_URL_WINDOWS_ARM64 = "/downloads/FileLore-Windows-Setup-arm64.exe"

/** Human-readable size of the Windows arm64 installer. */
export const DOWNLOAD_SIZE_WINDOWS_ARM64 = "73.4 MB"

/** Standalone diagnostics collector for Windows support (writes a report to the Desktop). */
export const DOWNLOAD_URL_DIAGNOSE = "/downloads/FileLore-Diagnose.cmd"

/**
 * ☕ Donate link for the "Support FileLore" section.
 *
 * TODO: replace with your real Ko-fi / Buy Me a Coffee page URL before
 * shipping, e.g. "https://ko-fi.com/yourname". Until then the button
 * points at a placeholder that goes nowhere useful.
 */
export const DONATE_URL = "https://ko-fi.com/filelore"

/** Public source repo + Releases page. */
export const GITHUB_URL = "https://github.com/Francom121/FileLore"
export const GITHUB_RELEASES_URL = "https://github.com/Francom121/FileLore/releases"

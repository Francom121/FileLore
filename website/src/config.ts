/**
 * Site-wide constants.
 */

/** Direct download for the latest FileLore release (served from /public). */
export const DOWNLOAD_URL = "/downloads/FileLore-macOS.zip"

/** Human-readable size of the download zip, shown next to download buttons. */
export const DOWNLOAD_SIZE = "3.3 MB"

/** Direct download for the latest Windows (x64) release (served from /public). */
export const DOWNLOAD_URL_WINDOWS = "/downloads/FileLore-Windows-x64.zip"

/** Human-readable size of the Windows download zip. */
export const DOWNLOAD_SIZE_WINDOWS = "63.1 MB"

/** Standalone diagnostics collector for Windows support (writes a report to the Desktop). */
export const DOWNLOAD_URL_DIAGNOSE = "/downloads/FileLore-Diagnose.cmd"

/**
 * ☕ Donate link for the "Support FileLore" section.
 *
 * TODO: replace with your real Ko-fi / Buy Me a Coffee page URL before
 * shipping, e.g. "https://ko-fi.com/yourname". Until then the button
 * points at a placeholder that goes nowhere useful.
 */
export const DONATE_URL = "https://ko-fi.com/REPLACE_ME"
